{
  "name": "rulesets-sandbox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interactive sandbox for testing Rulesets compilation",
  "bin": {
    "rulesets-sandbox": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "rm -rf dist && bun build src/index.ts --outdir dist --target node",
    "start": "bun src/index.ts",
    "clean": "rm -rf dist output",
    "reset": "bun run clean && mkdir -p output examples",
    "ruleset": "bun run start run",
    "ruleset:clean": "bun run start clean"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "chalk": "^5.3.0"
  },
  "license": "MIT"
}
