{
  "name": "rulesets",
  "version": "0.1.0",
  "description": "A CommonMark-compliant rules compiler that lets you author a single source rules file in Markdown and compile it into destination-specific rules files",
  "type": "module",
  "private": true,
  "keywords": [
    "monorepo",
    "bun",
    "typescript",
    "turbo",
    "ai-rules",
    "markdown",
    "compiler"
  ],
  "author": "Matt Galligan <matt.galligan@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/outfitter-dev/rulesets.git"
  },
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "@types/node": "^24.2.1",
      "typescript": "^5.9.2",
      "type-fest": "^4.41.0",
      "vitest": "^3.2.4",
      "js-yaml": "^4.1.0",
      "@types/js-yaml": "^4.0.9"
    }
  },
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "test:ci": "turbo test --concurrency=1",
    "format": "bun x ultracite@latest format && prettier --write \"**/*.{json,yaml,yml}\" && markdownlint-cli2 --fix \"**/*.{md,mdx,mdc}\"",
    "format:check": "bun x ultracite@latest lint",
    "format:check:all": "bun x ultracite@latest lint && prettier --check \"**/*.{json,yaml,yml}\" && markdownlint-cli2 \"**/*.{md,mdx,mdc}\" && (stylelint \"**/*.css\" || echo 'No CSS files found')",
    "lint": "bun x ultracite@latest lint && markdownlint-cli2 \"**/*.{md,mdx,mdc}\" && (stylelint \"**/*.css\" || echo 'No CSS files found')",
    "lint:fix": "bun x ultracite@latest format && prettier --write \"**/*.{json,yaml,yml}\" && markdownlint-cli2 --fix \"**/*.{md,mdx,mdc}\" && (stylelint --fix \"**/*.css\" || echo 'No CSS files found')",
    "lint:md": "markdownlint-cli2 \"**/*.{md,mdx,mdc}\"",
    "lint:md:fix": "markdownlint-cli2 --fix \"**/*.{md,mdx,mdc}\"",
    "lint:css": "stylelint \"**/*.css\"",
    "lint:css:fix": "stylelint --fix \"**/*.css\"",
    "lint:packages": "publint",
    "typecheck": "turbo typecheck",
    "typecheck:all": "tsc --noEmit",
    "ci:full": "bun run lint && bun run typecheck && bun run test:ci && bun run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "bun run build && changeset publish",
    "clean": "rm -rf dist .turbo node_modules bun.lockb",
    "clean:all": "bun run clean && bun run --filter '*' clean",
    "prepare": "lefthook install",
    "lefthook:run": "lefthook run"
  },
  "packageManager": "bun@1.2.19",
  "engines": {
    "node": ">=20.0.0",
    "bun": ">=1.2.0"
  },
  "trustedDependencies": [
    "@biomejs/biome",
    "lefthook"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.1.4",
    "@changesets/cli": "^2.29.5",
    "@commitlint/cli": "^19.8.1",
    "@types/bun": "^1.2.20",
    "@types/node": "^24.2.1",
    "@vitest/coverage-v8": "^3.2.4",
    "lefthook": "^1.12.2",
    "markdownlint-cli2": "^0.18.1",
    "pino": "^9.8.0",
    "pino-pretty": "^13.1.1",
    "prettier": "^3.6.2",
    "publint": "^0.3.12",
    "stylelint": "^16.23.1",
    "stylelint-config-standard": "^39.0.0",
    "syncpack": "^14.0.0-alpha.19",
    "turbo": "^2.5.5",
    "type-fest": "^4.41.0",
    "typedoc": "^0.28.10",
    "typescript": "^5.9.2",
    "ultracite": "^5.1.2",
    "vitest": "^3.2.4"
  }
}
